{
  "name": "list_directory",
  "description": "Lists the files and subdirectories at a given path. Use this to explore the filesystem — e.g. list the Desktop, Downloads, or any folder. Input path supports ~ for home directory.",
  "type": "file",
  "operation": "list",
  "input": {
    "path": {
      "type": "string",
      "required": true
    }
  },
  "output": {
    "files": { "type": "array" },
    "count": { "type": "number" },
    "path":  { "type": "string" }
  }
}
