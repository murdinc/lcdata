{
  "name": "voice_stt",
  "description": "Transcribes audio to text. Default: OpenAI Whisper (needs OPENAI_API_KEY). Switch provider to 'whisper-cpp' for fully local use — set whisperCppModel in lcdataenv.json or WHISPER_CPP_MODEL env var.",
  "type": "stt",
  "provider": "whisper-cpp",
  "language": "en",
  "input": {
    "audio_url": {
      "type": "string",
      "required": true
    }
  },
  "output": {
    "transcript":  { "type": "string" },
    "confidence":  { "type": "number" },
    "language":    { "type": "string" }
  }
}
