{
  "name": "format_search_results",
  "description": "Formats web search results into a voice sentence. Returns an honest error message if results are empty — never hallucinates.",
  "type": "command",
  "command": "python3",
  "args": ["format_search_results.py"],
  "env": {
    "RESULTS": "{{toJSON .input.results}}",
    "QUERY":   "{{.input.query}}"
  },
  "input": {
    "results": { "type": "array",  "required": true },
    "query":   { "type": "string", "required": false }
  },
  "output": {
    "stdout": { "type": "string" }
  }
}
